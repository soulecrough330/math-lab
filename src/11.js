`
console.log("Welcome to your math lab!");

// Define the function to calculate the area of a rectangle
function rectArea(width, height) {
  return width * height;
}

// Call the function with two input values and print the result
const result = rectArea(5, 4);
console.log("The area of the rectangle is " + result);`