// Example of generating random numbers within a specific range
const range = 10; // Define the lower and upper bounds
const randomNumber = Math.floor(Math.random() * (range - 1 + 1)) + 1;
console.log(randomNumber);
