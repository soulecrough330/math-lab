// Example 1: Simple loop to print numbers from 0 to 9
for(let i = 0; i <= 9; i++) {
    console.log(i);
}

// Example 2: For loop with a range and conditional statement
const fruits = ["apple", "banana", "cherry"];
for(let fruit of fruits) {
    if(fruit.includes("a")) {
        console.log(fruit);
    }
}
