// Example JavaScript code to simulate random number generation
function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Generate a random integer between 0 and 100 (inclusive)
console.log(getRandomInt(100));
