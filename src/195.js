function getRandomInt(max) {
    return Math.floor(Math.random() * max);
}

// Example usage:
const num1 = getRandomInt(10);
console.log(num1); // Output will be a random integer between 0 and 9

num2 = getRandomInt(5);
console.log(num2); // Output will be a random integer between 0 and 4
