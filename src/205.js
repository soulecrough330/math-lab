function findMax(num1, num2) {
  if (num1 > num2) {
    return num1;
  } else {
    return num2;
  }
}

function sumOfSquares(a, b) {
  return a * a + b * b;
}

function multiplyByTwo(x) {
  return x * 2;
}
