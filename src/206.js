function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
}

let secretCode = 789;

console.log("The random number generated by getRandomInt is: " + getRandomInt(100));

if (getRandomInt(5) === 3 || getRandomInt(4) === 3) {
    console.log("You're a genius!");
} else if (getRandomInt(6) === 2) {
    console.log("Keep practicing, you'll do better next time.");
}

const math = { // Define variables for mathematical operations
    add: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b
};

console.log("You can use these functions:");
for (const [key, value] of Object.entries(math)) {
    console.log(`- ${key}: ${value}`);
}

