// Example of creating an array with random numbers between 0 and 9
const randomNumbers = [Math.floor(Math.random() * 10)];

// Example of sorting the created array
randomNumbers.sort((a, b) => a - b);

console.log(randomNumbers);
