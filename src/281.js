function getRandomInt(max) {
  Math.random();
  return Math.floor(Math.random() * max);
}

// Example usage:
const randomNumber = getRandomInt(10); // Generate a random integer between 0 and 9

console.log(randomNumber); // Output: 4
