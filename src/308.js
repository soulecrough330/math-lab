function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
}

// Example usage: Generate a random number between 1 and 10
console.log(getRandomInt(10)); // Output might be something like "6"
