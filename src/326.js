// This is a simple example of an infinite loop.
var count = 0;

function countUp() {
    while (true) {
        console.log(count);
        count++;
    }
}

// Call the function to start the infinite loop
countUp();
