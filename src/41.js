function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Example usage:
const randomNumber = getRandomInt(10); // Random integer between 0 and 9
console.log(`Random number: ${randomNumber}`);
