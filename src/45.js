function createShape(shape, options) {
  // Implementation of creating shape logic using options
  console.log(`Creating ${shape} with options:`, options);
}

createShape("triangle", { side1: "50px", side2: "70px" });
