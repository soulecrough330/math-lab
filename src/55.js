function calculateArea(radius) {
  const area = Math.PI * radius ** 2;
  console.log("The area of the circle with radius " + radius + " is: " + area);
}

calculateArea(5); // Example usage: calculates the area of a circle with radius 5
